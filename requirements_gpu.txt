# GPU Acceleration Requirements for Kaggle T4x2

# Core GPU libraries
cupy-cuda11x>=12.0.0  # GPU-accelerated NumPy (CUDA 11.x for Kaggle)
cudf-cu11>=23.12.0    # RAPIDS GPU DataFrames
dask-cuda>=23.12.0    # Multi-GPU orchestration

# AI inference
onnxruntime-gpu>=1.16.0  # GPU inference engine

# Optional: TensorRT (if available on Kaggle)
# nvidia-tensorrt>=8.6.0

# Document processing (Docling)
docling>=1.0.0

# Validation and config
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0

# Data manipulation
pandas>=2.0.0
numpy>=1.24.0

# Utilities
tqdm>=4.65.0
click>=8.1.0

# LangChain integration
langchain>=0.1.0
langchain-community>=0.0.10

# Optional: OCR (if not using Docling's RapidOCR)
# rapidocr-onnxruntime>=1.3.0

# Logging and monitoring
rich>=13.0.0
psutil>=5.9.0

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
isort>=5.12.0
mypy>=1.5.0
